{
  "name": "@ga-ut/reverse-proxy",
  "version": "0.0.1",
  "description": "Standalone Bun-based reverse proxy with host routing, path/query preservation, X-Forwarded headers, and optional IP allowlist.",
  "repository": {
    "type": "git",
    "url": "https://github.com/ga-ut/reverse-proxy.git"
  },
  "homepage": "https://github.com/ga-ut/reverse-proxy#readme",
  "bugs": {
    "url": "https://github.com/ga-ut/reverse-proxy/issues"
  },
  "module": "src/index.ts",
  "type": "module",
  "main": "src/index.ts",
  "bin": {
    "reverse-proxy": "dist/reverse-proxy"
  },
  "files": [
    "dist",
    "proxy.config.example.json",
    "README.md"
  ],
  "scripts": {
    "dev": "bun --hot src/index.ts --no-service",
    "start": "bun src/index.ts --no-service",
    "build": "bun build src/index.ts --compile --target bun --outfile dist/reverse-proxy",
    "build:debug": "bun build src/index.ts --target bun --outfile dist/index.js",
    "clean": "rm -rf dist",
    "prepublishOnly": "bun run build"
  },
  "private": false,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.9.2"
  }
}
